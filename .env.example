# Base URL for sitemap.xml, robots.txt, and OG meta tags.
# Override in production via your deploy platform's env config.
PUBLIC_SITE_URL=http://localhost:5173

# SvelteKit base path for subdirectory deployments (e.g. GitHub Pages).
# Leave empty for root deployments.
BASE_PATH=
